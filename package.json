{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "ava tests/svg.js && lerna run test"
  },
  "devDependencies": {
    "ava": "^0.22.0",
    "lerna": "2.4.0",
    "svgo": "^0.7.2"
  }
}

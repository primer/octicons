{
  "private": true,
  "scripts": {
    "build": "script/build",
    "test": "npm run build && ava tests/js/svg.js"
  },
  "devDependencies": {
    "ava": "^0.22.0",
    "lerna": "^2.4.0",
    "svgo": "^0.7.2"
  }
}

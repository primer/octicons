{
  "name": "octicons_gem",
  "version": "4.0.2",
  "description": "Don't install",
  "scripts": {
    "postinstall": "bundle install --path ../vendor/bundle",
    "prepare": "cp -f ./build/data.json ./lib/",
    "postversion": "script/postversion",
    "postpublish": "gem build octicons.gemspec && echo gem push octicons-*.gem",
    "test": "rubocop && rake"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/primer/octicons.git"
  },
  "author": "GitHub, Inc.",
  "license": "MIT",
  "homepage": "https://github.com/primer/octicons",
  "dependencies": {
  }
}

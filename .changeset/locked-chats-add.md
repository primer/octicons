---
'@primer/octicons': minor
---

Add the 16px `terminal-locked` and `chat-add` icons for representing locked
terminal access and adding chats or messages. React and styled Octicons provide
`TerminalLockedIcon` and `ChatAddIcon`; `@primer/octicons-react-symbols`
provides `TerminalLockedSymbol` and `ChatAddSymbol`.

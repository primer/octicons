---
'@primer/octicons': minor
'@primer/octicons-react': minor
'@primer/styled-octicons': minor
---

Add the `chat-question` icon and `ChatQuestionIcon` export as the preferred names for `question-bubble` and `QuestionBubbleIcon`, which remain available as deprecated aliases for compatibility.

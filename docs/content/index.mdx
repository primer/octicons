import {Head, Header, Hero, Container} from '@primer/gatsby-theme-doctocat'
import {Link, Flex, Box} from '@primer/components'
import {graphql, Link as GatsbyLink} from 'gatsby'
import icons from '../../lib/build/data.json'
import Icon from '../src/components/icon'

export default function Layout({children}) {
  return (
    <Flex flexDirection="column" minHeight="100vh">
      <Head />
      <Header isSearchEnabled={false} />
      <Box width="100%">
        <Hero />
        <Container>{children}</Container>
      </Box>
    </Flex>
  )
}

<Flex flexWrap="wrap">
  {Object.values(icons).map(icon => (
    <Link as={GatsbyLink} display="block" p={3} color="inherit" to={icon.name}>
      <Flex>
        <Icon name={icon.name} />
      </Flex>
    </Link>
  ))}
</Flex>
